"""Core configuration and security utilities for the backend (Phase 1).
This package exposes `settings` and will contain helpers used by other modules.
"""

from .config import settings  # re-export for convenience
